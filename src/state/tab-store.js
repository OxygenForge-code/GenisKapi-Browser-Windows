class TabStore{constructor(){this.tabs=[];this.activeId=null;this.seq=0}create(url='about:blank',title='Yeni sekme'){const tab={id:`tab-${++this.seq}`,url,title,loading:false,muted:false,pinned:false};this.tabs.push(tab);this.activeId=tab.id;return tab}get(id){return this.tabs.find(t=>t.id===id)||null}activate(id){if(this.get(id))this.activeId=id;return this.get(id)}update(id,patch){const t=this.get(id);if(t)Object.assign(t,patch);return t}close(id){const i=this.tabs.findIndex(t=>t.id===id);if(i<0)return null;const [removed]=this.tabs.splice(i,1);if(this.activeId===id)this.activeId=this.tabs[Math.max(0,i-1)]?.id||null;return removed}list(){return this.tabs.slice()} }module.exports=TabStore;
